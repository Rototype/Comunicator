// ################# DEFINIZIONE AREA CONDIVISA #######################

#define ora_act	          (A[10]) // Ora attuale
#define min_act		      (A[11]) // Minuto attuale
#define gg_act		      (A[12]) // Giorno attuale
#define mm_act		      (A[13]) // Mese attuale
#define wday_act	      (A[14]) // Giorno della settimana attuale
#define anno_act          (A[15]) // Anno attuale
#define lampeggio         (A[16]) // Strobe ogni secondo 
#define lamp_vel          (A[17]) // Strobe ogni 500 msec
#define TAU_PLC           (A[18]) // Contatore dei TAU passati dalla partenza del programma
#define SEC_ON            (A[19]) // Contatore dei secondi passati dalla partenza del programma 
#define Versione_SWC      (A[20]) // Versione software Comunicator
#define Versione_SWH      (A[21]) // Versione software interfaccia HMI
